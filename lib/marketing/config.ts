// Where "Arrange Interview" (the main pre-sales CTA) points. Set
// NEXT_PUBLIC_BOOKING_URL in the environment to your Cal.com / Calendly link.
// Placeholder default until that's set — swap the tool anytime without touching
// the buttons.
export const BOOKING_URL =
  process.env.NEXT_PUBLIC_BOOKING_URL ?? 'https://cal.com/nobrandleftbehind/interview'
